{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <h1>Bienvenue sur Instep-Blog</h1>
    {# <div class="card" style="width: 18rem;">
        <img class="card-img-top" src="{{ themes.illustration }}" alt="{{ themes.title }}">
        <div class="card-body">
          <h5 class="card-title">{{ themes.title }}</h5>
          <p class="card-text">{{ theme.description }}</p>
          <a href="{{path('')}}" class="btn btn-primary">Voir tous les articles</a>
        </div>
    </div> #}
{% endblock %}
