<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <a class="navbar-brand ml-md-5 text-white" href="{{ path('admin_dashboard') }}"><h1>Administration My Blog</h1></a>
            <ul class="navbar-nav mr-auto ml-5 mt-2 mt-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if current_menu == 'dashboard' %}active text-decoration-underline{% endif %}" href="{{ path('admin_dashboard') }}">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if current_menu == 'themes' %}active text-decoration-underline{% endif %}" href="{{path('admin_themes')}}">Thèmes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if current_menu == 'articles' %}active text-decoration-underline{% endif %}" href="{{path('admin_articles')}}">Articles</a>
                </li>
            </ul>
        </div>
        <div>
            <a class="btn btn-light" target="_blank" rel="noopener noreferrer" href="{{ path('homepage') }}"><i class="fas fa-eye mr-2"></i>Voir le site</a>
            <a class="btn btn-danger" href="{{path('admin_account_logout')}}">Se déconnecter</a>
        </div>
    </nav>
</header>